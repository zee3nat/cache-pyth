[project]
name = "cache-pyth"
authors = []
description = "Decentralized price feed caching for Pyth Network on Stacks"
telemetry = false
requirements = []
[contracts.pyth-cache]
path = "contracts/pyth-cache.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
